/** @format */

import React from 'react';
import clsx from 'clsx';
//
const ContactUs = () => {
  return (
    <section
      className={clsx(
        `w-full flex flex-col justify-center items-center mt-[10px] lg:mt-[80px] md:px-8 px-4 border-2 h-[400px]`
      )}></section>
  );
};

export default ContactUs;
